<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.tiyansoft.abjad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launchere"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launchere"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity" android:screenOrientation="locked">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.Favorite_Activity" android:screenOrientation="locked">
        </activity>
        <activity android:name=".activities.About_Activity" android:screenOrientation="locked">
        </activity>
    </application>

</manifest>